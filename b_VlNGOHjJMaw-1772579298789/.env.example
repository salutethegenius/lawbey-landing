OPENWEBUI_URL=https://beta.lawbey.com/api/chat/completions
OPENWEBUI_API_KEY=
# Required. Use the retrieval-backed Open WebUI model (the one that emits source metadata).
# The homepage ask will fail rather than silently fall back to a generic chat model.
OPENWEBUI_MODEL=

# PostHog (Vercel Marketplace). Production-only; cookieless; no person profiles.
NEXT_PUBLIC_POSTHOG_PROJECT_TOKEN=
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com
